export default function IncidentsPage() {
  return (
    <div style={{ padding: 40, color: 'var(--text-secondary)' }}>
      No incident selected. Please select an active incident from the Command Center map.
    </div>
  );
}
